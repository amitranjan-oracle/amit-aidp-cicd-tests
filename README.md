# scratch

AIDP CI/CD test repository. A push to `main` reconciles the AIDP cluster, job,
and git folder via `deploy/aidp_deploy.py` (run by the self-hosted VM runner).
See `docs/oke-runner-setup.md` for the runner setup and `docs/` for the AIDP
issue write-ups.
